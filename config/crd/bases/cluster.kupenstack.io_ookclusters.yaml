
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: ookclusters.cluster.kupenstack.io
spec:
  group: cluster.kupenstack.io
  names:
    kind: OOKCluster
    listKind: OOKClusterList
    plural: ookclusters
    singular: ookcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: STATUS
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              nodes:
                description: By default all kubernetes nodes are part of OpenStack
                  cluster with default configurations. Nodes defines a list of all
                  nodes that have overriden default configurations.
                items:
                  properties:
                    disabled:
                      description: Default value is false. To remove node from cluster
                        set disabled to true.
                      type: boolean
                    name:
                      description: Name of node. Same as name of Kubernetes node.
                      type: string
                    profile:
                      description: 'Name of profile to use for this node. Default
                        value: default. For profile in different namespace use dns
                        notation of type profilename.namespace'
                      type: string
                    type:
                      default: compute
                      description: 'Type of node: "control" or "compute"'
                      enum:
                      - compute
                      - control
                      type: string
                  type: object
                type: array
            type: object
          status:
            properties:
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
